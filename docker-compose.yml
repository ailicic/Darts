services:
  darts:
    build: .
    image: darts-cutthroat
    container_name: darts
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - darts-data:/data
    restart: unless-stopped

volumes:
  darts-data:
